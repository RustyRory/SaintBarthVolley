name: Vérification de la structure du projet

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  check-structure:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Vérifier les fichiers et dossiers essentiels
        run: |
          MISSING=false

          echo "🔎 Vérification des fichiers racine..."
          for FILE in README.md CONTRIBUTING.md INSTALL.md LICENSE; do
            if [ ! -f "$FILE" ]; then
              echo "❌ Fichier manquant : $FILE"
              MISSING=true
            fi
          done

          echo "🔎 Vérification des dossiers MERN..."
          for DIR in frontend backend; do
            if [ ! -d "$DIR" ]; then
              echo "❌ Dossier manquant : $DIR"
              MISSING=true
            fi
          done

          echo "🔎 Vérification des fichiers internes du frontend..."
          if [ ! -f "frontend/package.json" ]; then
            echo "❌ frontend/package.json manquant"
            MISSING=true
          fi
          if [ ! -f "frontend/.env.example" ]; then
            echo "❌ frontend/.env.example manquant"
            MISSING=true
          fi

          echo "🔎 Vérification des fichiers internes du backend..."
          if [ ! -f "backend/package.json" ]; then
            echo "❌ backend/package.json manquant"
            MISSING=true
          fi
          if [ ! -f "backend/.env.example" ]; then
            echo "❌ backend/.env.example manquant"
            MISSING=true
          fi

          if [ "$MISSING" = true ]; then
            echo "💡 Corrigez les fichiers et dossiers manquants avant de merger la PR."
            exit 1
          else
            echo "✅ Tous les fichiers et dossiers essentiels sont présents."
          fi
