{
  "name": "frontend",
  "version": "0.0.2",
  "scripts": {
    "lint": "echo 'No frontend lint yet' && exit 0",
    "format": "echo 'No frontend format yet' && exit 0",
    "test": "echo 'No frontend tests yet' && exit 0",
    "build": "echo 'Frontend build placeholder' && exit 0"
  }
}
